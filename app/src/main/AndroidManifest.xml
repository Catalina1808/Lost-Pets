<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.lostmypet">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:icon="@drawable/icon_pet"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_pet"
        android:supportsRtl="true"
        android:theme="@style/Theme.LostMyPet">
        <activity
            android:name=".activities.ViewAnnouncementActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AllAnnouncementsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AddFirstLocationActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AddAnnouncementActivity"
            android:label="@string/title_add_announcement_activity" />
        <activity
            android:name=".activities.ProfileActivity"
            android:label="@string/title_activity_profile" />
        <activity
            android:name=".activities.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>